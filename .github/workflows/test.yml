name: test

on:

  pull_request:
    path: 
      - 'src/bibliography.xml'
  
  push:
    branches: 
      - 'main'
    path: 
      - 'src/bibliography.xml'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:

  checkout:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v2

    - name: install
      run: sudo apt-get install libxml2-utils

    - name: validate
      run: xmllint --noout --schema ./src/xsd/5.0/docbook.xsd ./src/bibliography.xml
